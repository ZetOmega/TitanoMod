package net.philippklei.titano.utils;

import net.minecraft.block.Block;

public class BlockUtils {
    public static boolean isOfType(Block block, Block type) {
        return block == type;
    }
}
